import { defineComponent, ref } from 'vue'
import { useRouter } from 'vue-router'
export default defineComponent({
  setup() {
    let num = (arr: string | number): string | number => {
      return arr
    }
  },
  render() {

  }
})